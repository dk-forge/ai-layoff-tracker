(() => { const api = altHealthData.apiUrl; const $ = id => document.getElementById(id); const esc = v => String(v ?? '—').replace(/[&<>]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;'}[c])); const fmt = v => v ? new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short',timeZone:'UTC'}).format(new Date(v))+' UTC' : 'Not yet reported';
const meta={edgar:['SEC EDGAR 8-K/6-K; US and foreign issuers','Twice daily'],newsapi:['Worldwide licensed news discovery','Twice daily'],gdelt:['Worldwide multilingual news discovery','Twice daily'],gdelt_historical:['Worldwide historical news recovery','Daily, success-anchored'],press_releases:['Reviewed company-controlled IR/newsroom feeds','Twice daily'],context_enrichment:['Existing source-linked records','Daily evidence-only']};
Promise.all(['quality-status','integrity-status','status','review-queue','benchmarks/challenger','benchmarks/recall'].map(x=>fetch(api+x+'?cb='+Date.now()).then(r=>r.ok?r.json():Promise.reject(x)))).then(([q,i,s,r,c,rec])=>{ const h=q.source_health||{}; const degraded=Object.values(h).filter(x=>x.status==='degraded').length; $('alt-health-updated').textContent=`Pipeline: ${s.pipeline_phase||'live'} · last data write ${fmt(s.last_updated)} · checked ${fmt(q.generated_at)}`; $('alt-health-summary').innerHTML=`<div><b>${i.canonical_events.toLocaleString()}</b><span>canonical events</span></div><div><b>${i.source_reports.toLocaleString()}</b><span>retained reports</span></div><div><b>${degraded}</b><span>degraded source${degraded===1?'':'s'}</span></div><div><b>${i.source_report_hashes_remaining.toLocaleString()}</b><span>evidence hashes pending</span></div>`; $('alt-health-sources').innerHTML=Object.entries(h).map(([id,x])=>{const m=meta[id]||['Operational collector','See source-health']; return `<tr><th>${esc(id)}</th><td>${esc(m[0])}</td><td>${esc(m[1])}</td><td><time>${esc(fmt(x.checked_at))}</time></td><td>${Number(x.entries||0).toLocaleString()} found</td><td><span class="alt-health-status alt-health-${esc(x.status)}">${esc(x.status)}</span> ${esc(x.detail||'')}</td></tr>`}).join('')||'<tr><td colspan="6">No collector reports yet.</td></tr>'; $('alt-health-workstreams').innerHTML=(q.workstreams||[]).map(x=>`<p><span class="alt-health-status alt-health-${esc(x.status)}">${esc(x.status)}</span> <b>${esc(x.id.replaceAll('_',' '))}</b><br>${esc(x.scope)}</p>`).join(''); const last=Array.isArray(c)&&c[0]?c[0]:null; $('alt-health-backlogs').innerHTML=`<p><b>Evidence hash backfill:</b> ${i.source_report_hashes_remaining.toLocaleString()} retained excerpts pending.</p><p><b>Editorial review:</b> ${(r.total||0).toLocaleString()} high-impact records queued.</p><p><b>Challenger:</b> ${last?`${Number(last.tracker_ai_primary_announced_us_employer_jobs_ytd).toLocaleString()} strict tracker vs ${Number(last.challenger_ai_jobs_ytd).toLocaleString()} benchmark`:'No retained comparison yet'}.</p><p><b>Country recall:</b> ${(rec||[]).length?'published sample available':'no current independently documented sample published'}.</p>`; }).catch(e=>{$('alt-health-updated').textContent='Health data could not be loaded. Please retry.'; console.error(e);}); })();
